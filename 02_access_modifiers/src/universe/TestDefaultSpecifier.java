package universe;

public class TestDefaultSpecifier {

	public static void main(String[] args) {
		// create an object (instance) of DefaultSpecifier
//		Default can't access outside the package
//		DefaultSpecifier access = new DefaultSpecifier(); compiler error
//		access.display();

	}

}
